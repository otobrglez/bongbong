// A placeholder app whose only job is to make Xcode's automatic signing
// mint the Apple Development certificate and the provisioning profile for
// com.otobrglez.bongbong on the Personal Team (tools/ios/sign.sh). The
// real game binary is signed with what this produces.
int main(void) { return 0; }
