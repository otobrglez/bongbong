[workspace]
members = ["cargo:."]

# Config for 'dist'
[dist]
# The preferred dist version to use in CI (Cargo.toml SemVer syntax)
cargo-dist-version = "0.32.0"
# CI backends to support
ci = "github"
# The installers to generate for each app
installers = []
# Target platforms to build apps for (Rust target-triple syntax)
targets = ["aarch64-apple-darwin", "x86_64-apple-darwin", "x86_64-unknown-linux-gnu", "x86_64-pc-windows-msvc"]
# Extra static files to include in each App (path relative to this Cargo.toml's dir)
include = ["static"]
# cargo-dist defaults to statically linking the Windows CRT (+crt-static),
# but raylib-sys builds raylib's C sources via the `cmake` crate, which does
# not follow Cargo's crt-static target feature and always compiles
# dynamically - the mismatch (static Rust std + dynamic raylib objects)
# fails the MSVC link with unresolved __imp_* CRT symbols (realloc, fopen,
# etc). Disabling this falls back to Rust's normal default (dynamic CRT),
# the same thing a plain `cargo build` on Windows already does - no new
# runtime requirement vs. today's dev builds.
msvc-crt-static = false

# raylib (via sola-raylib -> raylib-sys) needs X11/OpenGL/audio/input dev
# headers to build on Linux; macOS and Windows runners need nothing extra
# (Xcode/MSVC already have what's required). Same package list as
# sola-raylib's own working CI (../sola-raylib/.github/workflows/ci.yml),
# which builds this exact dependency across ubuntu-latest/-arm, macos-latest
# and windows-latest already - not a guess assembled from general raylib
# advice.
[dist.dependencies.apt]
libasound2-dev = '*'   # audio
libudev-dev = '*'      # gamepad/input
libx11-dev = '*'
libxcursor-dev = '*'
libxinerama-dev = '*'
libxrandr-dev = '*'
libxi-dev = '*'
libgl1-mesa-dev = '*'
